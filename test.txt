#define PI 3.14159
#define MAX 100


int main() {
    int limit = MAX;
    double circle = PI * 2;
    char *msg = HELLO;

    int sum = ADD(3, 4);

    /* This is a comment with MAX inside it — should be ignored */
    printf("%s\n", msg);
    return 0;
}

